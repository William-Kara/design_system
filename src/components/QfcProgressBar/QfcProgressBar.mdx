import { Meta, ArgTypes } from '@storybook/blocks';
import QfcProgressBar from '@/components/QfcProgressBar/QfcProgressBar.vue';

<Meta title="Elements/ProgressBar" />

## Description

The QfcProgressBar component is a customizable progress bar used to display the progress of different tasks.

## Props

The following props are available for the QfcProgressBar component:

<ArgTypes of={QfcProgressBar} />

You should add title to QfcProgressBar for better accessibility.
