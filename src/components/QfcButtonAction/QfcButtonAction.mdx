import { ArgTypes, Meta } from '@storybook/blocks';
import QfcButtonAction from '@/components/QfcButtonAction/QfcButtonAction.vue';

<Meta title="Elements/ButtonAction" />

## Description

The QfcButtonAction component is a button that can be used for any action. It embeds a notification element that can be toggled.

## Props

The following props are available for the QfcButtonAction component:

<ArgTypes of={QfcButtonAction} />
