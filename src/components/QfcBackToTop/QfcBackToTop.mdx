import { Meta } from '@storybook/blocks';

<Meta title="Elements/BackToTop" />

# Definition & Usage

A back-to-top button that allows visitors to quickly scroll back to the top of the page with a single click.

It only appears when the user have scroll down.
